# Vector of fruits
fruits <- c("apple", "orange", "banana", "grape", "kiwi")
# Vector of colors
colors <- c("red", "orange", "yellow", "purple", "green")

#' Both vectors have the same length, so to loop over
#' them in parallel we can loop over the length of just
#' one of the vectors

# Loop over the length of the fruits vector
for (i in seq(length(NAME OF FRUITS VECTOR))) {
  # Use paste0 to combine the words at the first
  # index int he vectors, the second, etc.
  fruits_colors <- paste0(VECTOR1, ",", VECTOR2)
}
